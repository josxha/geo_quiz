name: geo_quiz
description: Geography quizzes
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^3.0.0
  flutter_map_geojson: ^1.0.1
  latlong2: ^0.8.1
  flutter_map_tappable_polyline: ^4.0.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/world-administrative-boundaries.json
